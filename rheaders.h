#ifndef RESPONSE_KEYS_SIZE
  #define RESPONSE_KEYS_SIZE 7 // Всегда обновлять с обновлением response_keys
#endif
#ifndef SERVER_NAME
  #define SERVER_NAME "nkitty/0.1" // Рабочее название
  int get_str_time(char* dest,int max);
  int get_header_response(struct response_head head, char* dest);
#endif
